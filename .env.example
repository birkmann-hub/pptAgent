# Copy to .env and fill in your values
# Never commit the actual .env file

# OpenAI API key (required for AI-generated content)
# Without this, the agent falls back to professional placeholder content.
OPENAI_API_KEY=sk-...

# Optional: choose a specific model (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o
